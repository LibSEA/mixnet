pre-commit:
    jobs:
        - name: format
          glob: "*.go"
          run: golangci-lint fmt {staged_files}
        - name: lint
          glob: "*.go"
          run: golangci-lint run --fix

