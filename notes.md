The idea is the kbucket ranges are distances from identity node.

That way we don't have to do that left right business and can just
keep going to the next index.

That is because the leftmost bucket is always the closest.
